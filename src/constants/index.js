import URL from './url';

const Accessibility = {
  ADMIN: 'ADMIN',
  SUPERVISOR: 'SUPERVISOR',
  STAFF: 'STAFF',
};

const AlertMessage = {
  EMPTY_FORM: 'Please fill this form.',
};

export default { URL, Accessibility, AlertMessage };
